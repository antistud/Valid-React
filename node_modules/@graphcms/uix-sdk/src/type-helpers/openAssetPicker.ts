export type openAssetPicker = () => Promise<
  null | (Asset & Record<string, unknown>)
>;

export interface Asset {
  createdAt: string;
  fileName: string;
  handle: string;
  height?: number;
  id: string;
  mimeType?: string;
  size?: number;
  updatedAt: string;
  url: string;
  width?: number;
}
